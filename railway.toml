[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn noteshub.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/api/token/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
